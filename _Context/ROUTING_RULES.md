# Routing Rules

- For GitHub/backup tasks, read `_Agent/GITHUB_RULES.md` first.
- For old vault lookup, read `90_Archive_Index/Old Vault Index.md` first.
- For AI workflow tasks, read `07_AI_Workflows` and `_Context` first.
- For study tasks, read `_Context/LEARNING_MAP.md` first.
- For decision/review tasks, read `05_Decision` and `06_Review`.
- If task is ambiguous, ask the user which context to use.
- Do not scan `D:\Zhuan_Vault` by default.